# 端到端示例：从一句话到研发级特性需求

本文件演示本技能完整跑一遍的样子，供生成时参照"好的产出"标准。
示例中的配置区取值仅为占位演示，实际以用户配置为准。

> 本示例是**纯二开单层**（迎宾导览）案例。若需求**跨实现层**（动了二开层以外的固件/算法/硬件），完整跑法（含可行性证伪、跨部门协同视图、依赖顺序、阻塞降级、主档案+各层子需求档）见 `references/cross-layer-routing.md` 末尾的 set_microphone_beam_angle 端到端示例。

---

## 输入

> 客户说想要个迎宾导览功能。

## 澄清过程（交互式逐问，一次一个）

**复述确认**
> 我理解是：机器人在入口接待来访者，并主动带领其前往目的地。是这样吗？

— 用户：对。

> （**第 2 步分流**：通读后只有"迎宾导览"一个可独立成立的诉求，记"已确认单一特性"。**第 3 步归类**：见下，L2/L3 边界是首个阻塞缺口。**第 4 步跨层分解**：本特性在二开 SDK 层组合已有的导航/避障/定位能力，不新建固件/算法/硬件改动，记"单层特性（二开 SDK）"，跳过《跨层实现与跨部门协同》节，直接进第 5 步定位澄清。）

**第 5 步 · Q1（阻塞 · 层级归属）**
> 客户要的是一个集成商能拿去二次开发的能力包，还是一个开箱即用、直接部署的成品方案？这决定它归 L2 还是 L3。
> A. 能力包（集成商自己搭场景）　B. 成品方案　C. 帮我分析两种差异

— 用户：A，能力包。

**第 5 步 · Q2（阻塞 · 角色与场景）**
> 谁来调用这个能力包？是集成商的应用开发者在写自己的接待应用时调用，对吗？典型场景是哪种？
> A. 写字楼大堂接待　B. 展厅讲解导览　C. 政务/医院导引　D. 通用，不限场景

— 用户：主要是 B 展厅讲解，但希望通用。

**第 5 步 · Q3（阻塞 · 范围边界）**
> "导览"里，下面哪些算这个特性要做的，哪些明确不做？
> 候选：路径规划、避障行走、到点讲解（语音播报）、人脸/人形跟随、多人接待排队。

— 用户：路径规划、避障行走、到点触发讲解要做；讲解内容本身（讲什么）不做，由集成商传入；排队不做。

**第 5 步 · Q4（形态）**
> 这个能力包要同时支持仿真和真机，还是先真机？
> A. 两者都要　B. 先仿真　C. 先真机

— 用户：两者都要，验收要能在仿真里先跑通。

**第 5 步 · Q5（接口影响面 / 集成商分级 / 安全）**
> 面向哪一级集成商？另外，导览涉及机器人自主移动，速度/避障距离这类参数若开放覆盖，要不要限制在安全范围内？
> A. Associate（高度封装）　B. Professional（高层入口+关键参数可覆盖，覆盖值钳制在安全范围）　C. Expert（细粒度控制）

— 用户：Professional；参数可覆盖，但必须钳制在安全范围内。

> （第一层定位的阻塞性缺口已补齐，进入**第 6 步：第二层功能细节澄清**。该特性跨原型 A 运动/导航 + C 交互回调，从探针库取对应清单。）

**第 6 步 · Q6（细节 · 异常处理，A 原型）**
> 导览途中如果路径被堵（比如有人站在通道上），机器人应该怎么办？
> A. 原地等待，超时后上报　B. 自动绕行重规划　C. 直接中止本次导览

— 用户：先等待，超时（比如 10 秒）还堵就绕行；绕不过才上报中止。

**第 6 步 · Q7（细节 · 到达判定，A 原型）**
> 到达一个途经点的判定标准是什么？位置误差到多少算"到了"、需要朝向对准吗？
> A. 仅位置，误差阈值待定　B. 位置 + 朝向都要

— 用户：展厅讲解要面向展品，所以位置 + 朝向都要；具体阈值我回头让算法同学定，先标记。

**第 6 步 · Q8（细节 · 状态语义，A 原型）**
> 导览过程中支持暂停/继续吗？暂停时如果调用方又发了新的导览请求，怎么处理？
> A. 支持暂停/继续；暂停态拒绝新请求　B. 不支持暂停，只能停止重来

— 用户：支持暂停/继续；暂停态拒绝新请求，返回明确错误。

> （关键行为已补齐，阈值类细节打标记。**第 7 步：先过产品原则自检 gate，再按模板产出。**）

## 产出（第 7 步）

```markdown
# 特性需求：迎宾导览开发包（Welcome Navigation Package）

> 状态：待评审　｜　提出方：客户（展厅场景）　｜　日期：2026-06-29　｜　模板：完整（单层）

## 一句话定义
提供一个面向 Professional 级集成商的导览能力包：机器人按预设/动态路径带领访客行进、全程避障，并在指定途经点触发由集成商提供的讲解内容。

## 来源与背景（Why）
- 原始诉求：客户希望有"迎宾导览功能"。
- 业务场景：展厅讲解导览为主，但能力包需通用，可复用于大堂接待、政务导引等。
- 价值：把高频的"接待+带路+到点讲解"动作沉淀为可复用组件，降低集成商搭建接待类应用的成本。

## 能力定位
- 层级：L2（组合能力 / 应用级开发包）
- 能力域：跨域 —— 运动/导航域 + 感知域（途经点识别与避障）
- 跨域影响：依赖导航与感知两域的底层能力；讲解内容播放接口与音频/交互域对接但不属本特性范围。
- 跨层结论（第 4 步）：**单层特性（二开 SDK）** —— 在 SDK 层组合已有的导航/避障/定位能力，无固件/算法/硬件新建改动。

## 范围边界
- 包含（In Scope）：路径规划（基于预设途经点序列）、避障行走、到达途经点触发"讲解回调"、仿真与真机两套运行。
- 明确不做（Out of Scope）：
  - 讲解内容本身（讲什么）—— 由集成商通过回调传入
  - 多人接待 / 排队调度
  - 人脸识别身份核验
- 关联特性占位（来自需求分流、本轮不做）：无。

## 跨层实现与跨部门协同
单层特性（二开 SDK），无跨层协同。

## 开发者/集成商故事（按 P1/P2/P3 优先级）
- **[P1]** 作为集成商开发者，我希望用一个高层入口传入途经点序列即可启动导览，以便几行代码完成接待应用主流程。
  - 验收标准（含 DX 验收）：给定 N 个途经点，机器人按序到达全部点位、无碰撞、到点触发回调；接口命名/参数风格与同域既有导航接口一致，失败返回可定位根因的错误码。
  - 优先级依据：客户核心诉求（导览主流程），硬需求。
  - 仿真判据：样本 N=5 途经点 ｜ 前置：标注好途经点的仿真场景地图 ｜ 判定：到达率与碰撞由仿真器轨迹日志判 ｜ 阈值：到达率 100%、无穿模碰撞。
  - 真机判据：样本 N=5 ｜ 前置：对应实景地图 ｜ 判定：定位系统读数 + 人工观察 ｜ 阈值：到达定位误差 ≤ [待确认] cm、全程无接触式碰撞。
- **[P1]** 作为集成商开发者，我希望在到达每个途经点时收到回调，以便插入自己的讲解逻辑。
  - 验收标准：每个途经点到达后回调被调用且携带点位标识。
  - 优先级依据：与导览主流程同等核心，硬需求。
- **[P2]** 作为 Professional 集成商，我希望能覆盖关键参数（行进速度、避障距离阈值），以便适配不同场地。
  - 验收标准：传入自定义参数后行为按预期变化；超出安全范围的值被钳制并告警。
  - 优先级依据：适配性增强项。

### MVP 最小切分
- 本轮最小可交付集(MVP) = 两条 P1（按序到点导览 + 到点回调）。
- 砍到 MVP 后是否仍对客户成立：是 —— 展厅讲解主流程靠这两条即可闭环。
- 明确推迟到下一迭代：P2 参数覆盖（默认参数先够用）。

## 行为规格（Behavioral Spec）
- 正常流程：接收途经点序列 → 规划路径 → 依次行进（全程避障）→ 到达每点触发讲解回调 → 全部完成后结束会话。
- 状态与生命周期：`空闲 → 行进中 →（暂停）→ 行进中 → 结束/中止`。支持 pause/resume；暂停态拒绝新的 start 请求并返回明确错误码；stop 立即结束并复位到空闲。
- 异常与失败处理：
  - 路径被堵：原地等待 10s（默认，可调）→ 仍堵则尝试绕行重规划 → 绕不过则上报并中止。
  - 定位丢失：暂停行进并上报，触发重定位流程（[待确认] 是否本包负责）。
- 边界与边缘场景：途经点序列为空 → 拒绝并报错；单点 → 走到即结束；中途 stop → 安全停车并复位。
- 参数细则：行进速度（默认/范围 [待确认]，覆盖值钳制在安全范围）、避障安全距离（默认/范围 [待确认]）、堵塞等待超时（默认 10s，可调）。
- 时序与性能：到点回调需在到达后 ≤ [待确认] ms 内触发。
- 并发与可重入：同一时刻仅允许一个导览会话；行进态/暂停态收到新 start 一律拒绝。
- 可观测性：面向调用方[当前状态查询、当前途经点序号、到点/异常事件回调] ｜ 面向平台运营[导览成功率、中止率、平均完成时长埋点]。

## 非功能性需求(NFR) 与上线就绪
- 安全与权限：对 Professional 级开放；自主移动属有风险动作，速度/避障距离的覆盖值必须钳制在安全范围、越界告警；保留急停优先级最高。
- 数据与隐私/合规：本特性不采集个人/敏感数据（途经点为预设坐标，讲解内容由集成商提供）。
- 运营可观测：上线后看导览成功率/中止原因分布/采纳集成商数；中止率突增告警。
- 性能与容量[按需]：避障实时性与到点回调时延 [待确认]，由算法同学给基准。

## 接口契约草案（签名级，不写实现）
- 新增接口（示意）：
  - `start_tour(waypoints, on_arrive_callback, options?)` ｜ 输入：途经点序列、到点回调、可选参数 ｜ 输出：导览会话句柄 ｜ 错误码：路径不可达 / 途经点非法 / 会话已存在 ｜ 异步
  - `pause_tour(handle)` / `resume_tour(handle)` / `stop_tour(handle)`
  - `get_tour_status(handle)` ｜ 输出：状态 + 当前途经点序号
  - `options` 可覆盖：行进速度、避障距离阈值、堵塞等待超时
- 命名与绑定约定：全小写下划线、词意完整、禁用缩写；与同域既有导航接口风格一致；C++ core + pybind11，回调遵循现有事件回调风格。

## 依赖与前置条件
- 依赖的已有底层能力：导航/路径规划、避障行走、定位（均已存在）。
- 需先建的能力：途经点抽象与序列编排（若尚无）。
- 硬件 / 仿真前提：需有对应场景地图；仿真侧需途经点标注支持。
- 外部依赖：讲解内容由集成商侧提供，不在本包内。

## 影响评估 · 兼容 · 发布策略
- API 预算影响：新增约 6 个方法（上限 300，占用后约 2%）。
  - 是否考虑过复用/合并：start/pause/resume/stop 为一套会话生命周期，已是最小集，不再合并。
- 向后兼容：纯新增，不破坏现有接口。
- 发布策略[按需]：可对 Professional 集成商灰度；默认参数开箱可用。
- 文档与认证影响：需新增"导览开发包"开发者文档；建议纳入 Professional 级集成商认证示例。

## 产品原则自检（Product Principles Gate）
本特性已逐条过内置默认 10 条原则，无 ⚠（安全默认：参数覆盖已钳制；范围纪律：Out-of-Scope 明确、MVP 已切；单一职责：仅做"带路+到点交出控制"）。

## 关键决策记录
| 决策点 | 结论 | 依据 / 来源 | 涉及原则 |
| --- | --- | --- | --- |
| 归 L2 还是 L3 | L2 能力包 | 客户明确要"集成商二次开发用" | 1 分层完整性 |
| 讲解内容是否本特性做 | 不做，回调交出 | 澄清 Q3，保持能力包通用 | 2 单一职责 / 8 范围纪律 |
| 排队/多人接待 | 不做 | 澄清 Q3 划出边界 | 8 范围纪律 |
| 形态 | 仿真+真机，仿真先行验收 | 澄清 Q4 | — |
| 集成商分级 | Professional，高层入口+关键参数可覆盖 | 澄清 Q5 | 5 DX |
| 参数覆盖安全性 | 覆盖值钳制在安全范围、越界告警 | 澄清 Q5 | 6 安全默认 |
| 路径被堵策略 | 等待10s→绕行→中止 | 澄清 Q6 | — |
| 到达判定 | 位置+朝向都要 | 澄清 Q7（阈值待算法定） | — |
| 暂停语义 | 支持暂停/继续，暂停态拒新请求 | 澄清 Q8 | — |

## 遗留待确认
- [ ] 真机到达定位误差阈值（cm）与朝向阈值 ｜ 定责人：算法同学 ｜ 定法：真机标定 ｜ 期限：下次评审前
- [ ] 行进速度 / 避障距离的默认值与可调安全范围
- [ ] 到点回调触发的时延上限（ms）
- [ ] 定位丢失后的重定位是否由本包负责
- [ ] 途经点是否支持运行时增删
```
